#!/bin/sh
# postinst script for jarvis
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    configure)
        # Soft link our Jarvis Demo Application XML file.
        if [ ! -e /etc/jarvis5/demo.xml ]; then
            ln -s /usr/share/jarvis5/demo/etc/demo.xml /etc/jarvis5/demo.xml
        fi

        # Copy the demo database.
        if [ ! -e /var/lib/jarvis5/demo/demo.db ]; then
            mkdir -p /var/lib/jarvis5/demo
            cp /usr/share/jarvis5/demo/db/demo-empty.db /var/lib/jarvis5/demo/demo.db

            set +e
            ID=`id -u www-data || id -u apache`;
            set -e
            if [ -n "$ID" ]; then
                chown -R $ID /var/lib/jarvis5/demo
            fi
        fi

        # Link in our Jarvis Apache configuration files to Apache - pre apache 2.4
        if [ -d /etc/apache2/conf.d/ ] && [ ! -e /etc/apache2/conf.d/jarvis5.conf ] && [ ! -e /etc/apache2/conf.d/jarvis5-mod-perl.conf ] ; then
            ln -s /etc/jarvis5/apache/jarvis5.conf /etc/apache2/conf.d/jarvis5.conf
        fi
        # Link in our Jarvis Apache configuration files to Apache - apache 2.4+
        if [ -d /etc/apache2/conf-available/ ] && [ ! -e /etc/apache2/conf-available/jarvis5.conf ] && [ ! -e /etc/apache2/conf-available/jarvis5-mod-perl.conf ] ; then
            cp /etc/jarvis5/apache/jarvis5.conf /etc/apache2/conf-available/jarvis5.conf
            ln -s /etc/apache2/conf-available/jarvis5.conf /etc/apache2/conf-enabled/jarvis5.conf
            a2enmod cgi
        fi

	# Don't restart apache.  It's not nice.
        # [ -x `which apache2ctl` ] && apache2ctl restart
	echo "Apache requires a restart for changes to take effect."
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
