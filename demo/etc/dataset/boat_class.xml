<dataset serial="id" read="*" write="*">
    <select>
SELECT id, class, active,
    COALESCE (description, '') AS description
FROM boat_class
ORDER BY class
    </select>
    <update>
UPDATE boat_class
SET class = BTRIM ({{class}}),
    active = {{active}},
    description = NULLIF (BTRIM ({{description}}), '')
WHERE id = {{id}};
    </update>
    <insert returning="yes">
INSERT INTO boat_class (class, active, description)
VALUES (BTRIM ({{class}}), {{active}}, NULLIF (BTRIM ({{description}}), ''))
RETURNING {{_record_id}}::integer as _record_id, id;
    </insert>
    <delete>
DELETE FROM item_type
WHERE id = {{id}};
    </delete>
</dataset>
