<dataset read="*" write="*">
    <select>
SELECT id, class, active,
    COALESCE (description, '') AS description
FROM boat_class
ORDER BY class
    </select>
    <update>
UPDATE boat_class
SET class = BTRIM ({{class}}),
    active = {{active}},
    description = NULLIF (BTRIM ({{description}}), ''),
    change_user = {{__username}},
    change_date = now()
WHERE id = {{id}};
    </update>
    <insert returning="yes">
INSERT INTO boat_class (class, active, description, change_user, change_date)
VALUES (BTRIM ({{class}}), {{active}}, NULLIF (BTRIM ({{description}}), ''), {{__username}}, now())
RETURNING {{_record_id}}::integer as _record_id, id;
    </insert>
    <delete>
DELETE FROM boat_class
WHERE id = {{id}};
    </delete>
</dataset>
